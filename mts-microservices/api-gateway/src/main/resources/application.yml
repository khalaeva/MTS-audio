server:
  port: 8765

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
#      routes:
#        - id: test
#          uri: lb://eclientlobby
#          predicates:
##            - Path=/main/test

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
